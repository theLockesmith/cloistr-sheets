/**
 * MenuBar.tsx — REMOVED by AppShell migration (feat/appshell-migration).
 *
 * The hand-rolled mobile hamburger button (aria-label="Open menu") has been
 * deleted.  @cloistr/ui's AppShell now owns the single mobile affordance for
 * OUR commands.  The desktop menu (File/Edit/View/Insert/Format/Data) is
 * rendered by AppShell's built-in menu bar.
 *
 * See src/components/Sheet.tsx → buildMenuSections() for the menu model.
 *
 * This file exists only because the sandbox prevents `rm` during the migration.
 * It should be deleted before merging.  It is not imported anywhere and will
 * be excluded from the bundle by Vite's tree-shaking.
 */

export {}
